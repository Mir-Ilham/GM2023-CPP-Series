#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl '\n'

const int SZ1 = 1e8; // 1 * 10^8
const int SZ2 = 1e9; // 1 * 10^9
const int SZ3 = 1e5; // 1 * 10^5
const int SZ4 = 1e6; // 1 * 10^6

// Global array
int arr1[SZ1]; // This array size for the global array is valid
//int arr2[SZ2]; // This array size for the global array is invalid

int main() {
    optimize();

    // Local array
    int arr3[SZ3]; // This array size for the local array is valid
    //int arr4[SZ4]; // This array size for the local array is invalid
    return 0;
}
